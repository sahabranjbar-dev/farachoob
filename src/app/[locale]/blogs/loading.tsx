import FullScreenLoading from "@/components/FullScreenLoading";
import React from "react";

const loading = () => {
  return <FullScreenLoading visibleBackground={false} />;
};

export default loading;
